/**
 * SECURITY CRITICAL:
 * This file is part of secret isolation architecture.
 * Server-only. Do NOT import this from client code (hooks, components, stores,
 * or any file that can be bundled for the browser).
 */

export { serverEnv, assertServerEnv } from '@/lib/server/env/serverEnv';
