/**
 * Серверная часть lib — только для Node.js (API / RSC).
 * Не импортировать из компонентов с 'use client' и из клиентского бандла.
 */
import 'server-only';

// Реэкспорт не делаем, чтобы не тянуть db/redis в бандл при случайном импорте.
// Импортируйте явно: import { db } from '@/lib/server/db/client' и т.д. из серверного кода.
export {};
